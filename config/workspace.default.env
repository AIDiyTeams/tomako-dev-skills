# tomako-dev-skills 默认配置（不含密钥）
# 复制为 workspace 根目录 .env.local 或在 shell profile 中 export

# 生产应用服务器（168）
SERVER_HOST=47.239.95.168
SERVER_USER=root
SERVER_PORT=22

# 团队统一 SSH 私钥路径（必须自行 export，不要提交真实路径到 git）
# export TOMAKO_SSH_KEY=~/.ssh/github_deploy_key

# cc-connect / Skills-OL（Phase 2）
# SKILLS_OL_HOST=8.210.246.124
# SKILLS_OL_USER=root

# 测试环境（Phase 2）
# TEST_SERVER_HOST=43.160.198.148

# 前端构建默认值
NEXT_PUBLIC_SITE_URL=https://tomako.ai
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_MOCK_API=false
